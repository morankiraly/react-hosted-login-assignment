import { ISubTenant, ISubTenantTree, ITenantsResponse } from '@frontegg/rest-api';
export type { ISubTenant };
export interface TenantsState {
    tenants: ITenantsResponse[];
    loading: boolean;
    error?: any;
    subTenants: ISubTenant[];
    tenantTree: ISubTenantTree | null;
}
